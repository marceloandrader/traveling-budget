import { budgetFor } from '../../../helpers/budget-for';
import { module, test } from 'qunit';

module('Unit | Helper | budget for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = budgetFor(42);
  assert.ok(result);
});
