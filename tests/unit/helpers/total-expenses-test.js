import { totalExpenses } from '../../../helpers/total-expenses';
import { module, test } from 'qunit';

module('Unit | Helper | total expenses');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalExpenses(42);
  assert.ok(result);
});
